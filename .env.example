# Copy to .env and fill in.
# Vite inlines VITE_* vars into the client bundle at build time — these are
# NOT secret in deployed builds; they only keep the key out of the Git repo.
# For a truly private key, put it behind a backend proxy instead.
VITE_DEEPSEEK_API_KEY=
